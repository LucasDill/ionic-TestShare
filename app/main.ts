import { platformBrowserDynamic } from '@angular/platform-browser-dynamic';// this is standard across many ionic applications 

import { AppModule } from './app.module';

platformBrowserDynamic().bootstrapModule(AppModule);
